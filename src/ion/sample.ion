
//function bar(a: 1 .. 3)
//    if a < -1
//        return a
//    return a

let a = 10
let b = a
let c = b

function bar(a: 1 .. 3)
    var x: 0 .. 20 = 5
    if a < 2
        x += a
        return a
    else
        x += 20
    return x

