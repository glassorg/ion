
function bar(a: -2.0 .. 3.0)
    //  if an if ends with a return always then convert to an if else
    if a < 0.0
        return a
    //if b < 0.0
    //    return b
    //let c = a ** b
    return a
