
//  [Integer[],Integer]{ .[1] >= 0, .[1] < .[0].length}
//  function get(array: Integer[], index: 0 .. array.length)
//  bug, don't replace declared type with inferred type.

copy = (from: Float[], to: Float[]{ length == from.length }) ->
    for i in 0 .. < from.length
        //  we should know that the lengths are the same here
        //  next, let's make this thingy not require the i < to.length check.
        if i < to.length
            to[i] = from[i]
    return to
