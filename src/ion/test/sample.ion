
//  [Integer[],Integer]{ .[1] >= 0, .[1] < .[0].length}
//  function get(array: Integer[], index: 0 .. array.length)
//  bug, don't replace declared type with inferred type.

struct Vector2
    x: Float
    y: Float

a = 1.0 + 2.0
//  this + override prevents +(Float,Float) above from resolving
`+` = (a: Vector2, b: Vector2) -> a.x + b.x + a.y + b.y
v = Vector2(1.0, 2.0) + Vector2(3.0, 4.0)

