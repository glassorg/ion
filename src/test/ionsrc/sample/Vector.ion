import
    ion.*

export
    class Vector
        let x: Number = 0
        let y: Number = 0

    let toString = (value: String | Number) =>
        if value is String
            "String"
            let x = 12
        else
            "Number"

    let add = (a, b) =>
        if a < 0
            a + b
        else if a > 0
            a - b
        else
            0
